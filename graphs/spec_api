send GET request to webui/json
with parameters:

'date_from': date unix timestamp for first sample if available (eg: 1390000000)
'date_to': date unix timestamp for last sample if available (eg: 1400000000)
'file': filename with extension (eg: bus_cif_2x.mp4)
'metric': for eg: psnr, ssim
'git_url': git url of the encoder

receiving:

git_url: git url of the encoder
file: parent filename with extension (eg: bus_cif.mp4)
realfile: real filename with extension (eg: bus_cif_2x.mp4)
date: added date
metric: for eg: psnr, ssim
bitrate: bitrate of the media in kbps
value: value for the requested metric
git_commit: git commit of the encoder used for the sample

==

send GET request to webui/json_encoders
with no parameter:

receiving:

a list of encoders

==

send GET request to webui/json_samplers
with no parameter:

receiving:

a list of parent samples (suppose no transcode otherwise behavior is not undefined)
